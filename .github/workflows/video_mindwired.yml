name: "Video: MindWired (Long-Form)"
on:
  schedule:
    - cron: '30 21 * * *'  # 17:30 EDT (psychology evening US)
  workflow_dispatch:
jobs:
  video:
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: chill_orbit
    secrets: inherit
