name: "Video: EspacioInteligente (Long-Form)"
on:
  schedule:
    - cron: '0 23 * * *'   # 19:00 EDT (prime US LF)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: hogarinteligente_lf
    secrets: inherit
