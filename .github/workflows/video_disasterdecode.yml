name: "Video: DisasterDecode (Long-Form)"
on:
  schedule:
    - cron: '30 21 * * *'
  workflow_dispatch:
jobs:
  video:
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: calm_earth
    secrets: inherit
