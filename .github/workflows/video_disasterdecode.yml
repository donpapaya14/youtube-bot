name: "Video: DisasterDecode (Long-Form)"
on:
  schedule:
    - cron: '0 21 * * *'   # 17:00 EDT (after-work documentary US)
  workflow_dispatch:
jobs:
  video:
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: calm_earth
    secrets: inherit
