name: "Video: FinanzasClara"
on:
  # PAUSADO 2026-05-28: canal muerto (~3 views, 6 subs). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '0 13 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: finanzas_clara
    secrets: inherit
