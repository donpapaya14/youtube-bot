name: "Video: FinanzasClara"
on:
  schedule:
    - cron: '0 13 * * *'   # 09:00 EDT (mañana US — commute)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: finanzas_clara
    secrets: inherit
