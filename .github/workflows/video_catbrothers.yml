name: "Video: CatBrothers"
on:
  schedule:
    - cron: '0 13 * * *'
    - cron: '0 19 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: catbrothers
    secrets: inherit
