name: "Video: CatBrothers"
on:
  schedule:
    - cron: '15 13 * * *'  # 09:15 EDT
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: catbrothers
    secrets: inherit
