name: "Video: CatBrothers"
on:
  schedule:
    - cron: '0 19 * * *'
  workflow_dispatch:
jobs:
  video:
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: catbrothers
    secrets: inherit
