name: "Video: CatBrothers"
on:
  schedule:
    - cron: '0 13 * * *'   # reducido 2/día→1/día 2026-05-28 (shorts avg ~5 views; el LF rinde mejor)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: catbrothers
    secrets: inherit
