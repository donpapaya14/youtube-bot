name: "Video: CatBrothers"
on:
  schedule:
    - cron: '30 9 * * *'
    - cron: '30 17 * * *'
  workflow_dispatch:
jobs:
  video:
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: catbrothers
    secrets: inherit
