name: "Video: SaludLongevidad (Long-Form)"
on:
  # PAUSADO 2026-05-28: canal enterrado (~0 views LF). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: salud_longevidad_lf
    secrets: inherit
