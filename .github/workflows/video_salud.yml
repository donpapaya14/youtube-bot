name: "Video: SaludLongevidad"
on:
  # PAUSADO 2026-05-28: canal enterrado (~1 view recientes, 62 subs). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '30 6 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: salud_longevidad
    secrets: inherit
