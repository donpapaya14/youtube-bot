name: "Video: SaludLongevidad"
on:
  schedule:
    - cron: '30 6 * * *'   # 08:30 España (mañana)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: salud_longevidad
    secrets: inherit
