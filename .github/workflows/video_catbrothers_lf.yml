name: "Video: CatBrothers (Long-Form)"
on:
  schedule:
    - cron: '30 23 * * *'  # 19:30 EDT (prime US LF)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: catbrothers_lf
    secrets: inherit
