name: "Video: CashCafe (Long-Form)"
on:
  # DESACTIVADO 2026-05-28: LF sin tracción (~1 view vs 31 en shorts). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '0 4 * * *'    # 06:00 ES (LF amanecer)
  #   - cron: '0 17 * * *'   # 19:00 ES (LF prime time)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: cash_cafe
    secrets: inherit
