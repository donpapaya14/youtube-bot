name: "Video: CashCafe (Long-Form)"
on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: cash_cafe
    secrets: inherit
