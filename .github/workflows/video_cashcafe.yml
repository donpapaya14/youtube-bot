name: "Video: CashCafe (Long-Form)"
on:
  schedule:
    - cron: '0 1 * * *'    # 21:00 EDT (late prime US — cafe lovers wind-down)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: cash_cafe
    secrets: inherit
