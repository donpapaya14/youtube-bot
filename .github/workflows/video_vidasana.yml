name: "Video: VidaSana360"
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: vida_sana_360
    secrets: inherit
