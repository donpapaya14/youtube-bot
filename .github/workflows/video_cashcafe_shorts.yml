name: "Video: CashCafe (Shorts)"
on:
  schedule:
    - cron: '30 13 * * *'
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: cash_cafe_shorts
    secrets: inherit
