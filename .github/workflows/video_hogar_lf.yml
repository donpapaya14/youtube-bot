name: "Video: EspacioInteligente (Long-Form)"
on:
  # DESACTIVADO 2026-05-28: LF sin tracción (~1 view vs 104 en shorts). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '0 23 * * *'   # 19:00 EDT (prime US LF)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: hogarinteligente_lf
    secrets: inherit
