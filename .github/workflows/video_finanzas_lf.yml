name: "Video: FinanzasClara (Long-Form)"
on:
  # PAUSADO 2026-05-28: canal muerto (~3 views LF). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '30 13 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: finanzas_clara_lf
    secrets: inherit
