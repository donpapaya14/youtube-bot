name: "Video: DarkFiles"

on:
  schedule:
    # Diario a las 16:00 España (14:00 UTC)
    - cron: '0 14 * * *'
  workflow_dispatch:

jobs:
  video:
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: dark_files
    secrets: inherit
