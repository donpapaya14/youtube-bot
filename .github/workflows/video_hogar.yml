name: "Video: HogarInteligente"
on:
  schedule:
    - cron: '30 13 * * *'  # 09:30 EDT
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/video_engine.yml
    with:
      channel: hogarinteligente
    secrets: inherit
