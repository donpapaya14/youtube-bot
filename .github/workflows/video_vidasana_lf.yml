name: "Video: VidaSana360 (Long-Form)"
on:
  # DESACTIVADO 2026-05-28: LF sin tracción (~11 views vs 224 en shorts). Lanzable a mano (workflow_dispatch).
  # schedule:
  #   - cron: '0 18 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: vida_sana_360_lf
    secrets: inherit
