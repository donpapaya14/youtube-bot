name: "Video: VidaSana360 (Long-Form)"
on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: vida_sana_360_lf
    secrets: inherit
