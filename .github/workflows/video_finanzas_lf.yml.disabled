name: "Video: FinanzasClara (Long-Form)"
on:
  schedule:
    - cron: '30 22 * * *'  # 18:30 EDT (early evening US)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: finanzas_clara_lf
    secrets: inherit
