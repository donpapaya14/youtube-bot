name: "Video: DarkFiles"
on:
  schedule:
    - cron: '0 22 * * *'   # 18:00 EDT (true crime evening US)
  workflow_dispatch:
jobs:
  video:
    permissions:
      contents: write
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: dark_files
    secrets: inherit
