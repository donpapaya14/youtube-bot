name: "Video: MindWired"

on:
  schedule:
    - cron: '0 16 * * *'
  workflow_dispatch:

jobs:
  video:
    uses: ./.github/workflows/longform_engine.yml
    with:
      channel: chill_orbit
    secrets: inherit
